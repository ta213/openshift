apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: apicurio-registry
  namespace: argocd
spec:
  to:
    kind: Service
    name: apicurio-registry
  port:
    targetPort: 8080
  tls:
    termination: edge
