apiVersion: apps/v1
kind: Deployment
metadata:
  name: apicurio-registry
  namespace: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apicurio-registry
  template:
    metadata:
      labels:
        app: apicurio-registry
    spec:
      containers:
        - name: registry
          image: apicurio/apicurio-registry-sql:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          ports:
            - containerPort: 8080
          env:
            - name: REGISTRY_DATASOURCE_URL
              value: "jdbc:postgresql://apicurio-db:5432/apicurio"
            - name: REGISTRY_DATASOURCE_USERNAME
              value: "apicurio"
            - name: REGISTRY_DATASOURCE_PASSWORD
              value: "apicurio"
            - name: REGISTRY_DATASOURCE_DRIVER
              value: "org.postgresql.Driver"
